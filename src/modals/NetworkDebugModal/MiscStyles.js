import styled from 'styled-components'
import Button from "../../assets/buttons";

const BeginButton = styled(Button.FixedSmallButton)`
  margin-top: 18px;
`;

const S = { BeginButton };

export default S;