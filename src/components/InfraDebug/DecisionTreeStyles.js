import styled from 'styled-components'

const TreeContainer = styled.div`
  display: block;
  margin: 30px auto 0 auto;
  height: 80%;
  width: 80%;
`;

const S = {
  TreeContainer
};

export default S;