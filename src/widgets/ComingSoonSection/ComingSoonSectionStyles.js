import styled from "styled-components";

const Icon = styled.img`
  color: pink;
  display: block;
  margin-left: auto;
  margin-right: auto;
  height: 150px;
  margin-top: 12px;
`;

const Title = styled.p`
  width: 100%;
  text-align: center;
  font-size: 17px;
  color: tomato;
`;

const S = { Icon, Title };

export default S;